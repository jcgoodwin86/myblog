services:
  blog:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./content:/app/content # hot-reload posts without rebuilding
    environment:
      - PORT=8080
    restart: unless-stopped
